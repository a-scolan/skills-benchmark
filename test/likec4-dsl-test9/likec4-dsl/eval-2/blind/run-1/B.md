```bash
npx likec4 export png --theme dark --flat -f "overview*" -o ./images
```

If you are **not** running the command from the current project root, append the project path as the final argument:

```bash
npx likec4 export png --theme dark --flat -f "overview*" -o ./images <project-dir>
```