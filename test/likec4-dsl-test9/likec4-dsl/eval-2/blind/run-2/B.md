```bash
likec4 export png --theme dark --flat -f "overview*" -o ./images [project-dir]
```

`[project-dir]` is optional if you run the command from the current LikeC4 project directory; otherwise pass the project directory path explicitly.
