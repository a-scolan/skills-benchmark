`payment.api` is **valid** because LikeC4 identifiers can use letters, digits, and underscores, and dots are used to form hierarchical identifiers.

`1backend` is **invalid** because an identifier cannot start with a digit.

`payment_api` is **valid** because underscores are allowed in identifiers.

`payment-api` is **invalid** because hyphens are not allowed in LikeC4 identifiers.

## Rewritten invalid identifiers

- `1backend` → `backend1`
- `payment-api` → `payment_api`
