A
Only A keeps `customer -> frontend -> api` as a single chained expression while attaching the body specifically to the `frontend -> api` hop, whereas B puts the body on the wrong hop and C/D split the chain into separate expressions.
